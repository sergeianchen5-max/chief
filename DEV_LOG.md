# DEV LOG: ШЕФ ХОЛОДИЛЬНИК

Этот файл служит единым источником правды о состоянии проекта для ИИ-ассистентов и разработчиков.

## 🟢 Текущий статус: PHASE 4.0 - Monetization, DB & Vercel
**Дата:** Текущая
**Версия:** 2.0.0 (Next.js App Router)

## 🎯 Бизнес-цели
1. **Мультиплатформенность:** Web (PWA), iOS/Android (в планах), Telegram Bot.
2. **Монетизация:** Подписка PRO (ЮKassa) для безлимитной ИИ-генерации.
3. **Scale & Performance:** Supabase SSR (Auth + DB) на Edge/Vercel.

## 🛠 Технический стек
- **Frontend/Framework:** Next.js (App Router) + React + TailwindCSS.
- **Backend/DB:** Supabase (Auth, PostgreSQL DB, RLS, Edge Functions).
- **AI Strategy:** OpenRouter (доступ к LLM моделям, fallback механизмы).
- **Payments:** ЮKassa (Server Actions + Webhook).
- **Deployment:** Vercel.

## 📝 Changelog (История изменений)

### Session 4.0: Payments, AI Limits, Auth & Vercel Deploy
- [x] **Supabase Auth & DB Migration:** Настроена аутентификация через Supabase SSR (cookies), миграция LocalStorage данных гостя в облако (Supabase PostgreSQL `loadUserData`, `useSupabaseSync`).
- [x] **Hydration & Loading Optimizations:** Решена проблема `DOMException: The operation was aborted` при быстром пререндеринге (удален `AbortController` из `useUser.ts`). Улучшена скорость загрузки главной страницы — синхронное чтение `localStorage`. Ошибка бесконечной загрузки (`<Loader2 />`) в панели админа `/admin` предотвращена.
- [x] **Premium (PRO) Features & Limits:** Бесплатные аккаунты ограничены 5 генерациями ИИ-рецептов в день (cron/API limit `generation_count`). Платные PRO-аккаунты снимают ограничение.
- [x] **Monetization (YooKassa):** Разработана страница `/pricing`. Настроен API Server Action для создания платежа через ЮKassa. Реализован Webhook-обработчик (`/api/webhooks/yookassa`) для выдачи `is_pro = true` пользователям.
- [x] **Legal Pages & Cookies:** Добавлены страницы **Условия использования** (`/terms`) и **Политика конфиденциальности** (`/privacy`). Встроен Cookie Banner.
- [x] **Vercel Deployment Readiness:** Весь код, Webhooks, RLS политики в БД Supabase адаптированы и успешно собираются на Vercel.

### Session 3.0: Scaling Strategy
- [x] **Architecture Update:** Базовое планирование инфраструктуры с Load Balancer и базой данных PostgreSQL.

## 🚀 План действий (Roadmap)
1.  **Testing Yookassa (Vercel):** Провести реальный или тестовый платеж в боевой среде (на Vercel), чтобы убедиться в срабатывании Webhook'a и получении статуса PRO.
2.  **App & PWA Enhancements:** Настройка Service Worker (offline mode), уведомлений.
3.  **Analytics & Admin Control:** Углубление функционала `/admin`, аналитика покупок PRO и регистраций.

## ⚠️ Known Issues / Tech Debt
- **Local Dev vs Prod:** Webhooks от ЮKassa (и других провайдеров) можно тестировать только на Server-Side (Vercel) с активными публичными ключами. В локальной среде (`npm run dev`) webhook недоступен "снаружи" без туннеля.